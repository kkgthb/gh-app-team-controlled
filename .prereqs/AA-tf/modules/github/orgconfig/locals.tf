locals {
  my_gh_repo_b_name = "${var.workload_nickname}repob"
  my_gh_app_slug = "${var.workload_nickname}gha"
  my_gh_org_secret_name = "${var.workload_nickname}os"
  my_gh_repo_a_name = "${var.workload_nickname}repoa"
}
